1) рекурсивный процесс
(define (+ a b)
  (if (= a 0)
    b
    (inc (+ (dec a) b))))

2) итеративный процесс
(define (+ a b)
  (if (= a 0)
      b
      (+ (dec a) (inc b))))

1) (+ 4 5) -> (inc (inc (inc (inc 5))))

2) (+ 4 5) -> (+ 3 6) -> (+ 2 7) -> (+ 1 8) -> (+ 0 9) -> 9
